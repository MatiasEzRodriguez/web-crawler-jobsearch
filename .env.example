# Database Configuration
# For SQLite (local development), use: file:./prisma/dev.db
# For PostgreSQL (local), use: postgresql://user:password@localhost:5432/jobcrawler
# For Neon (GitHub Actions), use: postgresql://user:password@ep-xxxxx.region.neon.tech/dbname?sslmode=require&pgbouncer=true
# Note: Neon URL comes from the "Pooler Connection" endpoint configured for "Transaction" pooling
DATABASE_URL="file:./prisma/dev.db"

# Debug Mode
# Set to true for detailed console logging
DEBUG=false

# Optional: User Agent (some sites may require it)
# USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
